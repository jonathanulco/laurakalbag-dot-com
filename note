#!/bin/bash

# Uses Hugo to create a new note based on the current date
# e.g., new my-awesome-blog-post will create content/YYYY/MM/DD/HH/MM/_index.md

pathPrefix="notes/"
postPath=$(date +'%Y/%m/%d/%H/%M')
pathSuffix="/index.md"

postPath="$pathPrefix$postPath$pathSuffix"

hugo new $postPath
